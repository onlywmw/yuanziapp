# system.ai 默认实现（RulesProvider 规则兜底）纯标准库，零第三方依赖。
#
# 可选增强（Phase 2，默认不安装）：本地 ONNX 意图分类模型。
# 安装 onnxruntime 并设置环境变量 AI_MODEL_PATH 指向存在的本地模型文件后，
# get_provider() 自动启用 OnnxProvider；缺库 / 加载失败 / 推理未实现时
# 静默回退规则实现，输出 source 记 'rules'，不影响默认链路。
# onnxruntime>=1.17  # 可选增强，默认注释掉
