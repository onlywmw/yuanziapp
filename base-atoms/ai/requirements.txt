# system.ai 默认实现（RulesProvider 规则兜底）纯标准库，零第三方依赖。
#
# 可选增强（Phase 2，默认不安装）：本地 ONNX 语义意图识别，永远本地推理、
# 不上云。选型 BAAI/bge-small-zh-v1.5（512 维句嵌入）+ prototypes.json 原型库
# 余弦相似度；规则快路径优先，规则 miss 时才走模型，参数提取沿用规则正则。
# 启用步骤：
#   1) pip install onnxruntime（numpy 随其安装；运行期两者都按可选依赖处理）
#   2) python download_model.py 拉取 Xenova/bge-small-zh-v1.5 的 model.onnx + vocab.txt
#   3) 设置 AI_MODEL_PATH 指向本地 model.onnx（vocab.txt 需在同目录）
# 环境变量 AI_ONNX_THRESHOLD 可调相似度阈值（默认 0.55）。
# 缺库 / 模型缺失 / 加载失败 / 推理异常时静默回退规则实现，
# 输出 source 记 'rules'，不影响默认链路。
# onnxruntime>=1.17  # 可选增强，默认注释掉
