# 质量治理组 · 设计文档精读审计报告

## 组概述

覆盖 9 份质量治理文档：架构评估、M4 质量评估、隔离评估与加固计划、Bug 登记册、清理计划、全量验证方案、两份冒烟规范。
总体结论：Schema 治理与清理计划已闭环，隔离加固 4 项落地 3 项；但质量门禁当前失效（BUG-034 CI 红灯未修），Bug 登记册与其自称的唯一事实来源 CSV 存在数据矛盾，registry.py 巨石拆分是最大未闭环项。
核验方式：全文精读 + Grep/Read/git 对照 `mcp-yuanzi-bridge/`、`yuanzi-cli/`、`qa/`、`.github/workflows/ci.yml`。

---

## ARCHITECTURE_ASSESSMENT.md

- **一句话定位**：Arch 对"30 测试失败"事件的根因分析，定性为流程/Schema/接口三重治理缺失，而非孤立 Bug。
- **核心决策清单**：
  - P0 分支保护：main 只收 PR + 1 Review + CI 通过，禁止直推
  - P0 Schema 单一权威源：DDL 只在 `migrations/*.sql`，registry.py 只调 `migrate(conn)`，废弃 `registry_meta`
  - P1 接口契约注册：公开函数契约注释 + `tests/test_contracts.py` 契约测试拦截
  - P2 版本跟踪表统一为 `schema_migrations`
- **关键契约**：以 `resolve_dependencies` 返回结构（ok/order/missing/cycles/deps）为契约示例，键名不可变。
- **文档与代码现状差距**：
  - 缺陷二、三均已闭环：`registry.py` 全文件 0 处 `CREATE TABLE`（grep 无匹配）；`ensure_registry_schema()` 是 `migrate(conn)` 兼容包装（registry.py:129-137）；`registry_meta` 全仓库无引用；`tests/test_contracts.py` 存在（13 用例）。migrations 已扩至 013。
  - 残留缺口：迁移非幂等——`005_content_hash_columns.sql` 裸 `ALTER TABLE ADD COLUMN`，本地陈旧 `registry.db`（被 `.gitignore` 的 `*.db` 忽略）有列但无迁移记录时，`import api`（api.py:445 模块级 `create_app` → :104 `migrate`）即抛 `RuntimeError: duplicate column name: content_hash`，导致 `test_api.py`/`test_auth.py` 当前收集失败（本机实测）。
  - 缺陷一（分支保护）仓库内无配置载体（无 CODEOWNERS/分支保护即代码），无法核验，只能看到 git log 仍是直推 main 风格。

## M4_QUALITY_ASSESSMENT.md

- **一句话定位**：M4 注册中心服务化的 v2 验收单，宣判 6/6 子任务通过、M4 不再阻塞。
- **核心决策清单**：
  - v2 基线：277 测试通过（bridge 224 / atoms 37 / cli 16），CI 绿灯
  - 关键修复：BUG-016/020/022/023/024/033 全部 Fixed
  - 残留移交：API 零认证(P0)→M6；冗余 DDL/遗留脚本(P2)→CLEANUP_PLAN；RBAC(P1)、审计哈希链(P2)→M6
- **关键契约**：`YUANZI_PROBE_ALLOWED_CIDR` 可配置回环白名单；`content_hash` 物理列 + backfill；审计节流。
- **文档与代码现状差距**：
  - 修复项代码证据齐全：probe CIDR 白名单（registry.py:626-687）、`backfill_content_hashes`（registry.py:1172）、`backfill_audit_chain`（registry.py:249）、008_audit_chain.sql 均存在。
  - "277 通过"是时点数字，现不可复现：本机 collect 实测 bridge 153 例 + 2 个收集错误（陈旧本地库 + 非幂等 005，见上节）、base-atoms 27 例、cli 因环境缺 typer 0 例；总数基线已漂移。
  - 残留表失实一处：称"RBAC 未实现 P1"，实际 `auth.py:22-25` 已实现 admin/registry/probe/viewer 四角色 + `require_role`，api.py 路由已挂 `dependencies=[admin]` 等；正确口径应为 BUG-025 的"12.5/14 条件性收尾"。

## ISOLATION_ASSESSMENT.md

- **一句话定位**：Arch 的隔离性炸点地图，判 ★★☆☆☆，指认 registry.py 巨石、atom_registry 共享状态、atoms VIEW 静默失败三大炸点。
- **核心决策清单**：炸点分级（表结构/签名/migrations/agent.db 🔴；基础原子/APK 隔离 🟢）；改造方向：拆模块、乐观锁、VIEW 防御、命名空间保护。
- **关键契约**：atoms VIEW 列结构（label/atom_type/endpoint/capabilities）对 APK GraphView 是隐式契约。
- **文档与代码现状差距**：
  - 判断属实且恶化：registry.py 从文档时 957 行涨至 **1234 行**，仍是单文件巨石（无 `registry/` 包）。
  - "基础原子隔离良好"属实：`base-atoms/` 13 个目录各有 core.py + server.py（ls 实测）。
  - "无并发保护"已过时：`version_counter` 乐观锁已进代码（见下节）。
  - 评估未覆盖的新风险：api.py:445 模块级 `create_app()` 使 `import api` 即对默认库跑迁移，测试收集与本地库状态耦合（本组实测炸点）。

## ISOLATION_HARDENING_PLAN.md

- **一句话定位**：隔离评估的 4 项加固实施方案，承诺"4 小时工作量、0 测试改动"。
- **核心决策清单**：加固1 registry 拆 7 模块（API 不变）；加固2 version_counter 乐观锁；加固3 atoms VIEW COALESCE；加固4 system./yuanzi. 命名空间保护。
- **关键契约**：迁移 009（乐观锁列）、010（VIEW 重建）；`ConcurrentModificationError` 重试 ≤3 次；`RESERVED_PREFIXES`。
- **文档与代码现状差距**（落地 3/4，文档状态失实）：
  - 加固2 已落地：实际为 `013_optimistic_lock.sql`（编号与文档的 009 不符）；registry.py:37 定义 `ConcurrentModificationError`，:775-820 写入路径带 expected_counter 条件。
  - 加固3 已落地：实际为 `012_atoms_view_coalesce.sql`，与文档 SQL 逐行一致。
  - 加固4 已落地：registry.py:34 `RESERVED_PREFIXES=("system.","yuanzi.")`，:430 submit 拦截返回 `reserved_namespace`；api.py:138-142 DELETE 路由 403。
  - 加固1 未落地：无 `registry/` 包，单文件反而从 957 行增至 1234 行，开口扩大。
  - 文档头部仍标 `📐 design-ready`、实施顺序表把 4 项全列待办——未回写执行状态；迁移编号（009/010 vs 实际 013/012）与"277 测试"基线均已过时。

## BUG_REGISTRY.md

- **一句话定位**：QA/Audit 的风险视图与裁决备案，自称执行状态以 `qa/tests/docs/templates/BUG-TRACKING-TEMPLATE.csv` 为唯一事实来源。
- **核心决策清单**：39 缺陷分级（P0×3 / P1×12 / P2×17 / P3×7）；Open 10 条权威清单；BUG-020 严重度分歧备案；裁决 2026-07-18-01（probe CIDR 契约链）。
- **关键契约**：ground truth 原则——CSV 为准，文档只是视图。
- **文档与代码现状差距**：
  - **文档与自身 CSV 矛盾**：CSV 实测 26 Fixed + 2 Fixed(partial) + **11 Open**；文档写 29 Fixed / 10 Open。BUG-030 在 CSV 中为 Open，文档却列入"已关闭索引"且 Open 清单漏列——违反自己声明的 ground truth 原则。
  - Open 项代码抽查属实：BUG-036（auth.py:91 仍 `credentials.credentials == env_token` 明文比较）；BUG-037（auth.py 全文无 audit/log 调用，401/403 确实不入审计）；BUG-034（ci.yml:30 引用 `atoms/atom-template/requirements.txt`、:61 跑 `pytest atoms/tests/`，而 `atoms/` 已不在 git HEAD——a73558a 提交明确 "remove legacy atoms/"，本地 atoms/ 仅为磁盘残留）；BUG-035（verify-all.sh 仍 `set -e` 后接 `$?` 判断的死分支，:26-45；只跑 3 个测试目录，"覆盖 4 目录"未闭环）。
  - qa/ 目录实质：`qa/tests/e2e/` 为 QA 执行脚本 11 个，`qa/tests/docs/templates/` 三件（CSV×2 + 周报模板），登记册引用的 CSV 路径属实存在。

## CLEANUP_PLAN.md

- **一句话定位**：原子入库链路清理方案，消除 DDL 三源分裂，锁定 `mcp_atoms.json → register_mcp_atoms.py → submit_atom()` 唯一入库路径。
- **核心决策清单**：删 `insert_atoms.py`、`import_mcp_servers.py` 及其测试（39 例）；`ensure_registry_schema()` 瘦身为 `migrate(conn)`；新增 `test_atoms_view.py` 防 VIEW 回退。
- **关键契约**：`migrations/*.sql` 为 DDL 唯一权威源；atoms 是 VIEW 不可写。
- **文档与代码现状差距**：
  - **5 步全部闭环**：两个遗留脚本及 `test_insert_atoms.py`/`test_import_mcp.py` 均已消失；`test_atoms_view.py` 存在；registry.py 无 `CREATE TABLE`；包装函数注释注明 BUG-026。
  - 计划后新增 005-013 共 9 个迁移（M5-M8 需求），单一权威原则被延续，未回潮。
  - 唯一缺陷：文档无状态头/完成回写，后来者无法从文档本身判断已执行；验证方案中 "grep -c 'CREATE TABLE' → 0" 的检查项已可被 verify-all.sh Phase 3 复用（属实存在）。

## FULL_VERIFICATION_PLAN.md

- **一句话定位**：M1-M7 七层验证矩阵（代码/注册中心/原子模型/通道/工作流/安全/文档一致性）+ 一键脚本设计。
- **核心决策清单**：6 个 Phase 执行顺序；合入 main 前必跑 Phase 1，发布前跑全部。
- **关键契约**：每层"验证项-方式-预期"表；`scripts/verify-all.sh` 调用 5 个 `verify_*.py` 子脚本。
- **文档与代码现状差距**：
  - 计划设计的 `scripts/verify_registry.py / verify_atom_model.py / verify_channels.py / verify_security.py / verify_docs.py` **全部不存在**（scripts/ 目录实测仅 verify-all.sh 与格式化/同步脚本）；实际 verify-all.sh 是另一套自编结构（5 个 Phase，无独立 Registry Phase），计划与实现两张皮。
  - "277 单测"基线过时；矩阵中通道/工作流层预期（5 种通道类型、DAG 重试 3 次）对应代码（workflow.py、engine.py）已存在但无对应 verify 脚本，矩阵仍停在纸面。
  - 文档无执行记录；其缺陷已由 BUG-035 部分立案（脚本三缺陷 + 声明失实），但计划文档本身未同步修订。

## SMOKE_TEST_SPEC.md（英文版）

- **一句话定位**：M3 任务 3.5 的原子冒烟门禁规范英文版，约定 `tests/test_core.py` + `handler() → {status, data/message}`。
- **核心决策清单**：Tier 1 五必测场景；按原子类型（function/external/mcp-server/data）附加要求；CI 独立 job 跑 `pytest atoms/tests/`。
- **关键契约**：输出格式 `{"status": "success|error", "data", "message"}`。
- **文档与代码现状差距**：
  - **整份文档已失效**：适用范围锚定 `atoms/` 示例原子，而 `atoms/` 已从 git 删除（a73558a）；CI 中它引用的两行（ci.yml:61-62 区域）正是 BUG-034 红灯根因。
  - 无任何代码按 `test_core.py` 约定执行——现行约定是 test_kernel/test_health（见下份）。
  - 未标 superseded/deprecated，与中文版同名并存，是典型的文档僵尸。

## atom-smoke-test-spec.md（中文版）

- **一句话定位**：现行有效的冒烟规范（v1.0），两层结构：离线内核冒烟 + 真实服务端点冒烟。
- **核心决策清单**：`python_script` 内核必须双层冒烟；禁用 import 清单（server/requests/urllib/http/socket）；端口读 meta.yaml 禁硬编码；`yuanzi test --fast` 只跑 `-k kernel`。
- **关键契约**：`tests/test_kernel.py` + `tests/test_health.py` 文件名是强约定，`yuanzi validate` 强制检查。
- **文档与代码现状差距**：
  - **已闭环，与代码逐条吻合**：`yuanzi-cli/yuanzi_cli/commands/validate.py:22-23` 检查两文件存在、:60-61/:118 禁用 import 检查；`commands/test.py:31` 实现 `--fast`；`yuanzi-atom-templates/{{cookiecutter.atom_id}}/tests/` 两文件齐备；`examples/com.example.sum/tests/` 含 test_kernel/test_health/test_security 三文件。
  - 轻微差距：规范 §7 称"冒烟失败 → pre-commit 拒绝提交"，`.pre-commit-config.yaml` 是否挂 `--fast` 未在本组核验范围内验证。

---

## 组内跨文档矛盾与不一致

1. **SMOKE_TEST_SPEC.md vs atom-smoke-test-spec.md**：同名规范两套互斥约定（test_core.py+handler+status/data vs test_kernel.py+test_health.py+handle+裸 dict 返回），前者已随 atoms/ 删除而失效但未标注，CI 仍在引用其路径（BUG-034 根因）。
2. **BUG_REGISTRY.md vs 其 CSV 事实来源**：统计 29F/10O vs CSV 实测 26F+2P/11O；BUG-030 状态直接矛盾（文档 Fixed / CSV Open）。
3. **ISOLATION_HARDENING_PLAN.md 状态头 vs 现实**：标 `design-ready`，实际 4 项加固已落地 3 项（迁移编号也与文档不符：009/010 → 实际 013/012）。
4. **M4_QUALITY_ASSESSMENT.md 残留表 vs 代码**：称"RBAC 未实现"，auth.py 四角色 RBAC 已在。
5. **FULL_VERIFICATION_PLAN.md 脚本设计 vs scripts/ 现实**：5 个 verify_*.py 均不存在，verify-all.sh 是另一套结构。
6. **基线数字系统性漂移**：registry.py 957→1234 行、"277 测试"在多份文档间互相引用但当前不可复现。

## 风险与建议（按优先级）

1. P0｜修 BUG-034：ci.yml 删除 `atoms/atom-template/requirements.txt` 安装行与 `pytest atoms/tests/` job（或恢复目录），质量门禁失效期间一切"绿灯"声明无效。
2. P0｜同步 BUG_REGISTRY.md 与 CSV（BUG-030 改回 Open、修正计数），或删除文档内统计只留 CSV 指针，消除双事实源。
3. P1｜迁移幂等化（005 等 ADD COLUMN 加存在性检查）+ api.py 取消模块级 `create_app()` 自动迁移默认库，消除"本地陈旧 .db 炸测试收集"陷阱。
4. P1｜SMOKE_TEST_SPEC.md 头部标注 superseded 并指向 atom-smoke-test-spec.md，或删除。
5. P1｜执行加固1：registry.py（1234 行）拆包，是当前最大结构开口，且随 M5-M8 功能持续恶化。
6. P2｜修 verify-all.sh（BUG-035）：去掉 `set -e`+`$?` 死分支，测试目录与 FULL_VERIFICATION_PLAN 对齐，并回填计划文档的脚本清单。
7. P2｜建立计划/评估类文档的完成回写规则（状态头 + 完成日期 + 实际迁移/提交编号），ISOLATION_HARDENING_PLAN 与 CLEANUP_PLAN 均缺。
8. P3｜ARCHITECTURE_ASSESSMENT 的分支保护结论仓库内不可核验，建议补 CODEOWNERS 或在文档登记 GitHub 配置时间与截图存档。
