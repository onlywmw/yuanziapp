# 09 · DESIGN_EXPERIENCE_SCENARIOS.md 增量审计（体验场景优化）

> 审计日期：2026-07-19 ｜ 基线：HEAD（含 M8 接线 fbdeba5 ≡ 7b4690d、双 baseUrl f89fc00 ≡ 665ae03、CI 修复 b4dbfb0 ≡ aacb981，三组哈希 commit message 逐字相同，为同一工作的重提交）
> 代码证据域：`widgetmcp_src/java/com/nous/widgetmcp/`、`mcp-yuanzi-bridge/`（含 registry 包、migrations）

## 文档定位

产品视角的体验缺口清单：以 8 个用户场景（首启/分享/信任/时空/日记/原子协作/社交/离线）走查 Yuanzi，提炼 15 个编号缺口并给出 P0/P1/P2 优先级。纯产品文档，无技术契约、无接口规格、无 status 字段。

## 核心决策清单

- 15 个缺口按场景编号 1-15，总结处分级：P0 = 1/4/6/9/11/12，P1 = 5/7/10/13/15，P2 = 8/14
- 体验三主张：图谱即生命体（活动流光、呼吸、时间线）/ 市场即社区（分享、关注、品味相似）/ 离线即一等公民（本地续跑 + 恢复补执行）
- 文档自身瑕疵：缺口 3（空白状态的美）在总结分级清单中被遗漏（清单只含 14 个编号）；场景六示例链依赖的 sensor 原子（天气/位置/音乐）在 `base-atoms/` 与 `yuanzi-atoms/` 中均不存在（Grep 无匹配），属"示例都跑不起来"的前置缺口

## 缺口矩阵（已闭环 / 部分闭环 / 仍开口）

### 已闭环（0 项）

无。M8 接线（fbdeba5）、YuanziApi 双 baseUrl（f89fc00）、CI 修复（b4dbfb0）均不直接对应本文档任何一个缺口的完整闭环——本文档 15 个缺口全部落在产品/社交/感知层，而三次提交解决的是渲染管线、端口契约与质量门禁。

### 部分闭环（4 项）

| # | 缺口 | 已有（证据） | 仍缺 |
|---|---|---|---|
| 3 | 空白状态的美 | CENTER 呼吸光晕已实现且 M8 后可见（`ObsidianTemplate.kt:99-109`、`:382` breathFactor ±10%/3s；模板已在 `MainActivity.kt:96` 注册，fbdeba5） | "周围偶尔飘过微小光粒子"无实现：`emitFlow/emitBurst`（`ParticleSystem.kt:141-145`）全仓零调用源 |
| 6 | 权限透明度 | 数据模型已具备：`atom-registry-schema.json:203-211` 有 `permissions_required`/`network_access`/`data_sensitivity`；`register_mcp_atoms.py:328` 实际填充 `["network"]` | 安装前展示为零：APK 无市场页/安装流程（见 05-终端），权限字段无任何 UI 消费者 |
| 7 | 安全信号 | 签名链路存在：`registry/core.py:13,18` 注册时 `compute_signature` 落库；审计哈希链 `registry/audit.py:16-63` + `/audit/verify`（`api.py:433`） | "作者实名认证/开源标志/第三方 API 标志"零实现（`auth.py` 是 API token 体系，非作者身份）；无任何 UI 信号展示 |
| 12 | 原子活动视图 | M8 已装基础设施：`GraphView.sendDataFlow` 入口（`GraphView.kt:229`）、`installParticleSystem` 已接线（`MainActivity.kt:99`，fbdeba5）、粒子 update/render 循环（`GraphView.kt:96,408`） | 触发链全断：`sendDataFlow` 全仓零调用方；`ObsidianTemplate.onDataFlow` 空实现（`:254-256` 注释自承"留待粒子版本"）；无节点闪烁、无"N 个原子正在协作"指示器（Grep 无匹配） |

### 仍开口（11 项）

| # | 缺口 | 代码证据 |
|---|---|---|
| 1 | 引导式初始化 | Kotlin 全仓无 onboarding/welcome/first-launch 任何痕迹（Grep 仅命中 README "首次构建"） |
| 2 | 预置工作流模板 | workflows API 已具备（`api.py:349-394` + `migrations/009_workflows.sql`），但无预置模板、无一键安装、APK 无工作流页（`MainActivity` 仍三页） |
| 4 | 一键分享工作流 | 无分享端点/无 wf 链接；`federation/export`（`api.py:401`）是注册表级对等同步，非单工作流分享与缺原子适配安装 |
| 5 | 工作流市场 | `marketplace.py:154` `marketplace_board` 仅原子评分板，无工作流维度 |
| 8 | 原子行为日志 | `audit` 表仅记录注册中心管理动作（状态变更）与安全事件（BUG-037，`audit.py:22-63`）；无"读取位置 3 次"级运行时行为采集 |
| 9 | 时间感知 | 无原子时段绑定；Kotlin 侧仅 `YuanziPollScheduler` 轮询（`MainActivity.kt:65`），与"原子按时间激活"无关 |
| 10 | 地点感知 | geofence/location 在 Kotlin 与 Core 中零匹配（仅 BrowserActivity 的 `window.location.href`） |
| 11 | 个人时间线 | `workflow_runs` 表（`009_workflows.sql`）仅是工作流执行记录（`/workflows/{id}/runs`，`api.py:390`），无跨原子生活时间线形态与 UI |
| 13 | 关注作者 | 零代码：无 follow/subscription 表、端点、UI；市场页本身不存在 |
| 14 | 品味相似的人 | `recommend.py:36-81` 是依赖图信号（dependency/dependent/same_category）的原子搭配推荐（`/atoms/{id}/recommendations`），无人与人匹配，文档明确"不是算法推荐"故现有端点不算部分闭环 |
| 15 | 离线模式 | 同步层无离线队列/补执行/网络状态感知（`YuanziSyncService/YuanziSync/YuanziApi` grep offline/retry/queue 零匹配）；CENTER 断网灰态无（模板 grep gray/offline 零匹配）；f89fc00 双 baseUrl 只解决端口契约，与离线无关 |

## 与既有文档的兼容性

- 与 GRAPH_MIXER_MODEL/GRAPH_PARAMETER_PANEL：无矛盾。本文档未涉混音台/参数面板；其"图谱随时间/地点变化"（缺口 9/10）若实现需复用模板可见度通道，属增量需求而非冲突。
- 与 DESIGN_FRONTEND_UI / APK_ARCHITECTURE：无新矛盾。本文档要求的市场页/工作流画布恰是 05-终端已确认"APK 中不存在"的部分，方向一致（文档要建、代码没有）。
- 与 ATOM_SENSOR_LAYER / 原子分类 v2：场景四/六预设了 sensor→fusion→rule→actuator 链条可用，但对应原子未注册，本文档隐含依赖未被其引用文档兑现——跨文档承诺断裂，未见显式矛盾条款。
- 与 M6 安全文档：缺口 6/7 的字段与签名已在 M6 落地范围内，本文档的"安装前展示"是 M6 未承诺的新展示层需求，无矛盾。

## 与代码现状的差距（汇总）

15 缺口 = 0 已闭环 / 4 部分闭环（#3/#6/#7/#12，均只完成了数据模型或渲染基础设施层）/ 11 仍开口。缺口最密集的三个域：市场与社交（#4/#5/#13/#14 全开口，且赖以展示的 APK 市场页不存在）、时空感知（#9/#10 全开口，需 schema 扩展）、运行时可见性（#8/#11/#12，采集与触发链断裂）。证据见上方矩阵各行。

## 与既有审计结论的关系

- **印证** 05-终端 "市场页/工作流画布在 APK 中不存在"：本文档 P0 缺口 4（分享）与 P1 缺口 5/7/13 全部依赖该缺失页面，缺口优先级反过来抬高了 05-终端 P0 建议（构建指南之外）的市场页权重。
- **缓解** 03-图谱引擎 "无人调 installParticleSystem / 粒子系统死代码"：fbdeba5 已在 `MainActivity.kt:99` 完成安装与渲染循环接线，该条发现可关闭；但 03 报告 "onDataFlow 空实现" 依旧成立，且新增更精确的一层——入口 `sendDataFlow` 亦无调用方。
- **新增**（00-总览未覆盖）：11 个产品层缺口（#1/#2/#4/#5/#8/#9/#10/#11/#13/#14/#15）是既有 6 组偏架构/契约审计未触及的维度；其中 #9/#10 需要 atom 激活元数据 schema 扩展，成本高于一般 UI 缺口。
- **独立于** 00-总览 P1"混音台/参数驱动物理零实现"：该条已被 fbdeba5 闭环（03 组待复核确认），与本文档缺口无映射关系。

## 风险与建议（按优先级）

1. **P0** 缺口 12 触发源接线：工作流 run / agent event 回流 `sendDataFlow`（engine.run_workflow → Core `/agent/event` → APK 轮询），否则 M8 粒子基础设施仍是"装好不亮"的半死代码。
2. **P0** 缺口 2+4 低成本闭环：workflows 表/CRUD/validate 已齐备，先做"导出/导入 JSON + 内置 3 个预置模板"即可兑现 P0 级分享，无需等 yuanzi.app 短链服务。
3. **P1** 缺口 6/7 展示层与 05-终端市场页合并实施：schema 权限字段与签名数据已在库，只差一个市场页 UI，避免重复设计。
4. **P1** 缺口 15 离线队列：YuanziSync 层加失败入队 + 恢复重放，配 CENTER 网络状态色（模板层小改）。
5. **P2** 缺口 9/10 需先设计 atom activation 元数据（时段/地理围栏）并扩展 `atom-registry-schema.json`，再谈 APK 侧执行器。
6. **P2** 缺口 1 onboarding 为纯 APK 侧向导，可与预置模板（缺口 2）打包为一个首启流程。
7. **P2** 文档还债：补 status 字段、把遗漏的缺口 3 补入总结分级、为 15 缺口建 product-backlog 追踪表（当前无归宿，易被遗忘）。
8. **P3** 缺口 8/11 共用数据底座：在 audit 表与 workflow_runs 之上扩展 atom 运行时事件采集，一次设计同时喂饱行为日志与个人时间线。
